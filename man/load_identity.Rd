% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{load_identity}
\alias{load_identity}
\title{Load the two published name tables and build the identity lookup.}
\usage{
load_identity(
  cran_db_path,
  bioc_db_path,
  cran_table = "cran_names_all",
  bioc_table = "bioc_names_all"
)
}
\arguments{
\item{cran_db_path}{Path to the SQLite file holding the CRAN name table.}

\item{bioc_db_path}{Path to the SQLite file holding the Bioc name table.}

\item{cran_table}{CRAN table name (default "cran_names_all").}

\item{bioc_table}{Bioc table name (default "bioc_names_all").}
}
\value{
The \code{maps} object from \code{\link[=resolve_identity_set]{resolve_identity_set()}}.
}
\description{
Reads \code{name_lower}, \code{canonical_name}, and \code{identity_state} from the CRAN and
Bioc name tables (each an SQLite file the caller has already downloaded) and
returns the unified lookup from \code{\link[=resolve_identity_set]{resolve_identity_set()}}. Apply \code{\link[=check_size]{check_size()}}
to \code{maps$n_cran} and \code{maps$n_bioc} against per-source baselines before trusting
the result.
}
